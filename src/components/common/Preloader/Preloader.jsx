import preloader from '../../Users/preloader.svg'
import React from 'react'

let Preloader = (props) => {
  return <div>
    <img src={preloader} alt={'preloader.gif'}/>
  </div>
}

export default Preloader